{
  "name": "resume-ai",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/**\" run dev",
    "build": "pnpm run typecheck && pnpm -r --filter \"./apps/**\" --if-present run build",
    "typecheck:packages": "tsc --build",
    "typecheck": "pnpm run typecheck:packages && pnpm -r --filter \"./apps/**\" --filter \"./packages/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.4",
    "@tailwindcss/oxide-win32-x64-msvc": "^4.3.0",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}